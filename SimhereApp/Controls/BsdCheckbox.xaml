<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:telerikPrimitive="clr-namespace:Telerik.XamarinForms.Primitives;assembly=Telerik.XamarinForms.Primitives"
             x:Class="SimhereApp.Portable.Controls.BsdCheckbox">
    <ContentView.Content>
        <StackLayout x:Name="MainContainer" Orientation="Horizontal" HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" Padding="0" Spacing="{Binding Spacing}">
            <telerikPrimitive:RadCheckBox x:Name="checkBox" StrokeWidth="1" IsChecked="{Binding IsChecked}" UncheckedColor="{Binding UncheckedColor}" CheckedColor="{Binding CheckedColor}" VerticalOptions="FillAndExpand" HorizontalOptions="Start" IsEnabled="False"/>
            <Label x:Name="title" Text="{Binding Title}" TextColor="{Binding TitleColor}" VerticalOptions="FillAndExpand" HorizontalOptions="StartAndExpand" FontSize="Medium" Margin="0,1,0,0" HorizontalTextAlignment="Center"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>